"use client";
import { useEffect, useState } from "react";
export function Inspector() { const [enabled,setEnabled]=useState(false),[hover,setHover]=useState<HTMLElement|null>(null);useEffect(()=>{const onMessage=(e:MessageEvent)=>{if(e.data?.type==="atoms-inspect")setEnabled(!!e.data.enabled)};window.addEventListener("message",onMessage);return()=>window.removeEventListener("message",onMessage)},[]);useEffect(()=>{if(!enabled)return;const move=(e:MouseEvent)=>setHover(document.elementFromPoint(e.clientX,e.clientY) as HTMLElement);const click=(e:MouseEvent)=>{const el=document.elementFromPoint(e.clientX,e.clientY) as HTMLElement;e.preventDefault();e.stopPropagation();if(!el)return;const r=el.getBoundingClientRect(),s=getComputedStyle(el);window.parent.postMessage({type:"atoms-selected-ui",selected_ui:{tag:el.tagName.toLowerCase(),role:el.getAttribute("role"),text:el.textContent?.trim().slice(0,160),class:el.className,width:`${Math.round(r.width)}px`,backgroundColor:s.backgroundColor}},"*")};window.addEventListener("mousemove",move);window.addEventListener("click",click,true);return()=>{window.removeEventListener("mousemove",move);window.removeEventListener("click",click,true)}},[enabled]);if(!enabled||!hover)return null;const r=hover.getBoundingClientRect();return <div style={{position:"fixed",left:r.left,top:r.top,width:r.width,height:r.height,outline:"2px solid #5b5ce2",pointerEvents:"none",zIndex:2147483647}}/> }
